name: raspberrymonitor # you probably want to 'snapcraft register <name>'
base: core18 # the base snap is the execution environment for this snap
version: '1.0.0+18' # just for humans, typically '1.2+git' or '1.3.2'
summary: raspberry system monitor # 79 char long summary
description: |
  raspberry system monitor build with flutter

grade: stable # must be 'stable' to release into candidate/stable channels
confinement: strict # use 'strict' once you have the right plugs and slots

parts:
  raspberrymonitor:
    stage-packages:
    - libgl1
    - libgtk-3-dev
    plugin: dump
    source: build/linux/1.zip

apps:
  raspberrymonitor:
    command: ./release/raspberryMonitor
    plugs: [opengl, x11, network, network-bind]